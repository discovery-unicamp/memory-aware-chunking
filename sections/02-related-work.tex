\section{Related Work}
\label{sec:related-work}

Memory management in \ac{HPC} has evolved through predictive modeling, runtime adaptation, and data partitioning strategies.
This section surveys these approaches and positions memory-aware chunking within the distributed computing landscape.

\subsection{Memory Consumption Prediction}

\ac{HPC} systems traditionally predict memory consumption at the job level, leveraging historical execution logs to estimate resource requirements.
Rodrigues et al.~\cite{rodrigues2016} pioneered machine learning approaches for HPC memory consumption prediction, achieving 98\% classification accuracy using features such as user ID, requested processor count, and job queue information.
Their random forest models successfully categorized jobs into memory usage bins, enabling better scheduling decisions on IBM Blue Gene/Q systems.

Li et al.~\cite{li2019} extended this work by combining binary classification with specialized regression models for outlier cases.
Their two-stage approach first identifies whether a job will be memory-intensive, then applies targeted regressors trained on similar historical jobs.
While effective for recurring workloads, both approaches share fundamental limitations: they require extensive historical data, lack adaptability to new application types, and operate at too coarse a granularity to guide array-level memory management.

More recent work by Tanash et al.~\cite{tanash2021ampro} incorporates deep learning techniques, using LSTM networks to capture temporal patterns in job submission sequences.
However, the black-box nature of these models renders them unsuitable for the transparent, operator-specific predictions that scientific computing contexts require.

\subsection{Memory-Aware Scheduling}

Runtime memory adaptation focuses on dynamic response to memory pressure.
Cleo~\cite{khandelwal2020} implements cost-aware task migration for MapReduce, achieving 40\% reduction in job completion times by balancing memory load across clusters.

Mary and its variants~\cite{thamsen2017} take a learning-based approach to runtime adaptation, building online models of task memory consumption and adjusting resource allocation dynamically.
The Hugo* variant specifically targets heterogeneous clusters, adapting its scheduling decisions based on node-specific memory characteristics.
These systems demonstrate the value of runtime monitoring but operate at container or task granularity, too coarse for the array-level chunking decisions required in numerical computing.

Myung and Lee~\cite{myung2021} propose memory harvesting techniques that reclaim unused memory from over-provisioned VMs.
While effective in cloud environments, this approach assumes elastic infrastructure unavailable in many \ac{HPC} settings.
Fundamentally, these runtime systems complement rather than replace intelligent chunking: even perfect task migration cannot salvage a job where individual chunks exceed available memory.

\subsection{Chunking Techniques}

Data chunking strategies have evolved from simple uniform partitioning to sophisticated adaptive approaches, though most focus on I/O optimization rather than memory management.
Zhang et al.~\cite{zhang2019} developed an adaptive chunking system for HDF5 that optimizes storage layout based on access patterns, achieving 3× speedup for certain query workloads.
Their cost model considers disk seek times and compression ratios but does not account for in-memory processing requirements.

Tantisiriroj et al.~\cite{tantisiriroj2011} analyzed the duality between HDFS and PVFS chunking strategies, demonstrating that optimal chunk sizes depend heavily on workload characteristics.
Their empirical study revealed that the common 64~MB default chunk size often leads to poor performance, motivating workload-specific tuning.
However, their analysis exclusively considers I/O throughput rather than memory consumption during computation.

\dask's current auto-chunking implementation~\cite{rocklin2015dask} represents the state-of-the-art in production systems.
The heuristic targets a fixed 128~MB chunk size, attempting to balance scheduling overhead with memory usage.
While simple and often effective, this approach has critical limitations: it ignores operator-specific memory expansion, assumes uniform memory availability across workers, and provides no safety guarantees against OOM failures.

\subsection{Summary and Research Gap}

Table~\ref{tab:comparison} contrasts existing approaches that either operate too coarsely, react too late, or ignore memory considerations.
The proposed method uniquely combines shape-based prediction and operator awareness for proactive, fine-grained memory management.
By recognizing that memory patterns become predictable from input dimensions at the operator level, this approach provides memory safety guarantees without sacrificing \dask's simplicity.

\begin{table}[htb]
    \centering
    \caption{Comparison of memory management approaches in distributed computing}
    \label{tab:comparison}
    \begin{tabular}{p{7.5cm}ccc}
        \toprule
        \multirow{2}{*}{\textbf{Approach}} & \textbf{Memory} & \textbf{Operator} & \textbf{Chunk} \\
        & \textbf{Prediction} & \textbf{Aware} & \textbf{Sizing} \\
        \midrule
        Job-level prediction \newline
        \small{[Rodrigues et al. 2016, Li et al. 2019]} & Historical & No & N/A \\
        \addlinespace
        Runtime adaptation \newline
        \small{[Khandelwal et al. 2020, Thamsen et al. 2017]} & Reactive & No & N/A \\
        \addlinespace
        Storage-focused [Zhang et al. 2019] & None & No & Static \\
        \addlinespace
        Dask auto-chunking [Rocklin 2015] & None & No & Fixed \\
        \addlinespace
        \textbf{Memory-aware chunking (Proposed)} & \textbf{Proactive} & \textbf{Yes} & \textbf{Adaptive} \\
        \bottomrule
    \end{tabular}
\end{table}